import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuchen.onlinetestingaccount',
  appName: 'Online testing account',
  webDir: 'dist',
  android: {
    backgroundColor: '#0b1518',
  },
};

export default config;
